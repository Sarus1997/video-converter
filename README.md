# 🎬 Video Converter (HEVC → MP4)

โปรเจกต์นี้เป็น **Python script** สำหรับแปลงไฟล์วิดีโอ **HEVC / H.265** ให้เป็น **MP4 (H.264 + AAC)**  
เพื่อให้เล่นได้บนโปรแกรมและอุปกรณ์ทั่วไป

---

## 📋 สารบัญ

- [โครงสร้างโฟลเดอร์](#-โครงสร้างโฟลเดอร์)
- [สิ่งที่ต้องติดตั้ง](#-สิ่งที่ต้องติดตั้ง)
- [วิธีใช้งาน](#-วิธีใช้งาน)
- [ตัวเลือกเพิ่มเติม](#-ตัวเลือกเพิ่มเติม)

---

## 🗂 โครงสร้างโฟลเดอร์
```
C:\VideoConverter
│
├── main.py                  # ไฟล์หลักสำหรับรันโปรแกรม
├── converter.py             # โมดูลแปลงไฟล์
├── ffmpeg_utils.py          # ฟังก์ชันสำหรับ FFmpeg
├── input_videos\            # ใส่วิดีโอ HEVC/H.265 ที่ต้องการแปลง
│   └── video1.mkv
├── output_videos\           # เก็บไฟล์ MP4 หลังแปลง (สร้างอัตโนมัติ)
└── README.md
```

---

## ⚙️ สิ่งที่ต้องติดตั้ง

### 1. Python 3.8 ขึ้นไป
ดาวน์โหลดได้ที่ [python.org](https://www.python.org/downloads/)

- ติดตั้ง `argparse`, `subprocess`, `shutil`, `sys`, `pathlib`

### 2. FFmpeg
ดาวน์โหลด: [ffmpeg.org/download.html](https://ffmpeg.org/download.html)

### 3. FFmpeg for windows
ดาวน์โหลด: [https://www.gyan.dev/ffmpeg/builds/#release-builds](https://www.gyan.dev/ffmpeg/builds/#release-builds)

- มองหา ``ffmpeg-release-essentials.zip`

**วิธีติดตั้ง FFmpeg:**
- **Windows:** แตกไฟล์แล้วเพิ่ม path ของ `ffmpeg.exe` เข้า System PATH
- **macOS:** `brew install ffmpeg`
- **Linux:** `sudo apt install ffmpeg` หรือ `sudo yum install ffmpeg`

### 4. ติดตั้ง dependencies

- กดปุ่ม Windows + R
- พิมพ์ sysdm.cpl แล้วกด Enter
- ไปที่แท็บ Advanced → กดปุ่ม Environment Variables...
- ในช่อง "System variables" หา Path → คลิก Edit
- กด New แล้วใส่:

```
  C:\ffmpeg\bin
```

### 5. ตรวจสอบการติดตั้ง FFmpeg ทดสอบว่าใช้ได้

**เปิด Command Prompt (cmd) แล้วพิมพ์:**
```bash
ffmpeg -version
```

> **หมายเหตุ:** สคริปต์นี้ใช้ **built-in Python modules** เท่านั้น ไม่ต้องติดตั้ง dependencies เพิ่มเติม  
> (`argparse`, `subprocess`, `shutil`, `sys`, `pathlib`)

---

## 💻 วิธีใช้งาน

### 🎯 แปลงไฟล์เดี่ยว
```bash
python main.py input_videos\video1.mkv output_videos\video1.mp4
```

**พารามิเตอร์:**
- `input_videos\video1.mkv` → ไฟล์ต้นฉบับ
- `output_videos\video1.mp4` → ไฟล์ผลลัพธ์ (ถ้าไม่ระบุ จะใช้ชื่อเดียวกับ input เปลี่ยนนามสกุลเป็น `.mp4`)

**ตัวอย่าง:**
```bash
# ระบุ output
python main.py input_videos\myvideo.mkv output_videos\converted.mp4

# ไม่ระบุ output (ได้ชื่อเดิม)
python main.py input_videos\myvideo.mkv
```

---

### 📦 แปลงหลายไฟล์ในโฟลเดอร์ (Batch Mode)
```bash
python main.py input_videos --batch
```

**การทำงาน:**
- โปรแกรมจะวนลูปแปลงทุกไฟล์วิดีโอใน `input_videos`
- ไฟล์ผลลัพธ์ (MP4) จะถูกบันทึกในโฟลเดอร์เดียวกับไฟล์ต้นฉบับ
- รองรับไฟล์นามสกุล: `.mkv`, `.mov`, `.avi`, `.mp4`, `.m4v`

**ตัวอย่าง:**
```bash
python main.py input_videos --batch

# ระบุโฟลเดอร์ output
python main.py input_videos --batch --output output_videos
```

---

## 🎛 ตัวเลือกเพิ่มเติม

### ปรับแต่งคุณภาพและความเร็ว
```bash
python main.py input_videos --batch --crf 20 --preset medium --audio-bitrate 128k --threads 4
```

### ตารางพารามิเตอร์

| ตัวเลือก          | ค่าเริ่มต้น | คำอธิบาย                                                                 |
|-------------------|-------------|-------------------------------------------------------------------------|
| `--crf`           | `20`        | **คุณภาพวิดีโอ** (0-51)<br>• ต่ำ = คุณภาพสูง, ไฟล์ใหญ่<br>• แนะนำ: 18-23 |
| `--preset`        | `medium`    | **ความเร็วการ encode**<br>• `ultrafast`, `superfast`, `veryfast`, `faster`, `fast`, `medium`, `slow`, `slower`, `veryslow`<br>• เร็ว = คุณภาพต่ำกว่า |
| `--audio-bitrate` | `128k`      | **บิตเรตเสียง**<br>• แนะนำ: `128k` (standard), `192k` (high quality), `320k` (best) |
| `--threads`       | `0`         | **จำนวน threads**<br>• `0` = auto (ใช้ทุก CPU cores)<br>• ระบุเลข = จำกัด threads |
| `--no-overwrite`  | `False`     | **ป้องกันเขียนทับไฟล์**<br>• ถ้ามีไฟล์ output อยู่แล้ว จะข้าม |

### ตัวอย่างการใช้งานขั้นสูง
```bash
# คุณภาพสูงสุด, ช้า
python main.py input_videos --batch --crf 18 --preset veryslow --audio-bitrate 192k

# แปลงเร็ว, คุณภาพปกติ
python main.py input_videos --batch --crf 23 --preset veryfast --audio-bitrate 128k

# ไม่เขียนทับไฟล์เดิม
python main.py input_videos --batch --no-overwrite

# จำกัด CPU ใช้ 2 cores
python main.py input_videos --batch --threads 2
```

---

## 📊 คำแนะนำ CRF และ Preset

### CRF (Constant Rate Factor)
- **18-20:** คุณภาพดีมาก, เหมาะกับไฟล์ต้นฉบับ
- **21-23:** คุณภาพดี, ใช้งานทั่วไป (แนะนำ)
- **24-28:** คุณภาพปานกลาง, ลดขนาดไฟล์

### Preset
- **fast - ultrafast:** แปลงเร็ว, ไฟล์ใหญ่กว่า
- **medium:** สมดุล (แนะนำ)
- **slow - veryslow:** แปลงช้า, ไฟล์เล็กที่สุด

---

## 🚀 เริ่มต้นใช้งาน
```bash
# 1. Clone หรือดาวน์โหลดโปรเจกต์
cd C:\VideoConverter

# 2. วางไฟล์วิดีโอใน input_videos\
# 3. รันคำสั่ง
python main.py input_videos --batch

# 4. รอการแปลงเสร็จ ไฟล์จะอยู่ใน output_videos\
```

---

## 📝 หมายเหตุ

- รองรับไฟล์ขนาดใหญ่ (> 4GB)
- แปลงแบบ lossless metadata (รักษาข้อมูล metadata ไว้)
- ไฟล์ output จะมีขนาดเล็กกว่าไฟล์ต้นฉบับโดยทั่วไป

---

## 🆘 แก้ไขปัญหา

**ปัญหา: `ffmpeg: command not found`**
- ตรวจสอบว่าติดตั้ง FFmpeg แล้วและอยู่ใน System PATH

**ปัญหา: แปลงช้ามาก**
- ใช้ `--preset fast` หรือ `--preset veryfast`
- ลด `--threads` ถ้า CPU ใช้งานหนัก

**ปัญหา: ไฟล์ output มีขนาดใหญ่**
- เพิ่มค่า `--crf` เป็น 23-25
- ใช้ `--preset slower`

---

## 📄 License

โปรเจกต์นี้เป็น open source สามารถนำไปใช้และแก้ไขได้ตามต้องการ

[MIT License](./LICENSE)